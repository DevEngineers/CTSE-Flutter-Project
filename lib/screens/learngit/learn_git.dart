import 'package:flutter/material.dart';

class LearnGitPage extends StatelessWidget {
  const LearnGitPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Learn Git page"));
  }
}
